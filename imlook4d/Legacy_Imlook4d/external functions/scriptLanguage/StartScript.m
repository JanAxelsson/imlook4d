    % Start a typical script
    % The script should be ended with the command EndScript
    
    % Store variables (so we can clear all but these)
    StoreVariables;
    
    % Store current
    handleToOriginal=imlook4d_current_handle;
% 
%     % Make a duplicate to work on
%     Duplicate           % Make a copy of imlook4d instance
%     MakeCurrent         % Rename newHandle to imlook4d_current_handle
%     Export              % Export variables

    % Make a duplicate to work on
    DuplicateOriginal          % Make a copy of imlook4d instance