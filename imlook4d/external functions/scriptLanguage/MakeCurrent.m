    % Make newHandle and newHandles the current handles to operate on
    % (imlook4d_current_handle, imlook4d_current_handles)
    imlook4d_current_handle=newHandle;
    imlook4d_current_handles=newHandles;
    %guidata(imlook4d_current_handle, imlook4d_current_handles);
    
    clear newHandle newHandles